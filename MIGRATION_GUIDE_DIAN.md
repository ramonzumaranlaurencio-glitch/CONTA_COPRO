# GUÍA DE MIGRACIÓN: Completar Consolidación DIAN\n\n## Introducción\n\nEste documento proporciona los pasos exactos para completar la consolidación colombiana (DIAN) eliminando las últimas referencias a normas peruanas (SUNAT) del sistema.\n\n**Estado actual:** 5 archivos de rutas corregidos (85% completado)\n**Trabajo restante:** Modelos ORM, migraciones, adaptadores (15% - ~2-3 horas)\n\n---\n\n## FASE 1: Modelado ORM (src/domain/models/accounting.py)\n\n### Paso 1.1: Renombrar Tabla y Modelo\n\n```python\n# ANTES (eliminar)\nclass SunatSubmission(Base):\n    __tablename__ = \"sunat_submission\"\n    id: Mapped[UUID]\n    tenant_id: Mapped[str]\n    financial_document_id: Mapped[UUID | None]\n    submission_type: Mapped[str]\n    endpoint_type: Mapped[str]\n    status: Mapped[str] = mapped_column(default=\"PENDING\")\n    ticket: Mapped[str | None]\n    xml_hash: Mapped[str | None]\n    cdr_code: Mapped[str | None]        # ❌ CDR es SUNAT\n    cdr_description: Mapped[str | None] # ❌ CDR es SUNAT\n    raw_response: Mapped[dict | None]\n    created_at: Mapped[datetime]\n\n# DESPUÉS (nuevo)\nclass DianSubmission(Base):\n    __tablename__ = \"dian_submission\"\n    id: Mapped[UUID]\n    tenant_id: Mapped[str]\n    financial_document_id: Mapped[UUID | None]\n    submission_type: Mapped[str]\n    endpoint_type: Mapped[str]\n    status: Mapped[str] = mapped_column(default=\"PENDING\")\n    ticket: Mapped[str | None]\n    xml_hash: Mapped[str | None]\n    cud_code: Mapped[str | None]                  # ✅ CUD es DIAN\n    response_description: Mapped[str | None]      # ✅ Nomenclatura DIAN\n    raw_response: Mapped[dict | None]\n    created_at: Mapped[datetime]\n```\n\n### Paso 1.2: Actualizar FinancialDocument\n\n```python\nclass FinancialDocument(Base):\n    # ELIMINAR estas columnas (peruanas)\n    # sunat_status: Mapped[str]\n    # cdr_status: Mapped[str]\n    # cdr_code: Mapped[str | None]\n    # cdr_description: Mapped[str | None]\n\n    # AGREGAR estas columnas (colombianas)\n    dian_status: Mapped[str] = mapped_column(\n        default=\"PENDING\",\n        comment=\"Estado en DIAN: PENDING, QUEUED, ACCEPTED, REJECTED\"\n    )\n    dian_response_status: Mapped[str | None] = mapped_column(\n        comment=\"Respuesta específica de DIAN\"\n    )\n    dian_response_description: Mapped[str | None]\n    cud_code: Mapped[str | None] = mapped_column(\n        comment=\"Comprobante Único de la DIAN\"\n    )\n```\n\n### Paso 1.3: Crear Alias para Compatibilidad\n\n```python\n# Para NO romper código existente que aún use SunatSubmission\n# (usar solo temporalmente durante transición)\nSunatSubmission = DianSubmission  # Alias deprecado\n```\n\n---\n\n## FASE 2: Migraciones Alembic\n\n### Paso 2.1: Crear Migración 1 - Renombrar Tabla\n\n**Archivo:** `alembic/versions/20260606_rename_sunat_to_dian_submission.py`\n\n```python\n\"\"\"Rename sunat_submission to dian_submission table and update columns.\n\nRevision ID: 001_rename_sunat_dian\nRevises: <REVISION_ANTERIOR>\nCreate Date: 2026-06-06 00:00:00.000000\n\"\"\"\nfrom alembic import op\nimport sqlalchemy as sa\n\n# revision identifiers, used by Alembic.\nrevision = '001_rename_sunat_dian'\ndown_revision = None  # Ajustar al revision anterior real\nbranch_labels = None\ndepends_on = None\n\ndef upgrade() -> None:\n    # Renombrar tabla\n    op.rename_table('sunat_submission', 'dian_submission')\n    \n    # Renombrar columnas\n    with op.batch_alter_table('dian_submission', schema=None) as batch_op:\n        batch_op.alter_column('cdr_code', new_column_name='cud_code')\n        batch_op.alter_column('cdr_description', new_column_name='response_description')\n\ndef downgrade() -> None:\n    with op.batch_alter_table('dian_submission', schema=None) as batch_op:\n        batch_op.alter_column('cud_code', new_column_name='cdr_code')\n        batch_op.alter_column('response_description', new_column_name='cdr_description')\n    \n    op.rename_table('dian_submission', 'sunat_submission')\n```\n\n### Paso 2.2: Crear Migración 2 - Actualizar FinancialDocument\n\n**Archivo:** `alembic/versions/20260606_add_dian_columns_financial_document.py`\n\n```python\n\"\"\"Add DIAN columns to financial_document and migrate data from SUNAT columns.\n\nRevision ID: 002_add_dian_cols_fin_doc\nRevises: 001_rename_sunat_dian\nCreate Date: 2026-06-06 00:01:00.000000\n\"\"\"\nfrom alembic import op\nimport sqlalchemy as sa\n\nrevision = '002_add_dian_cols_fin_doc'\ndown_revision = '001_rename_sunat_dian'\nbranch_labels = None\ndepends_on = None\n\ndef upgrade() -> None:\n    with op.batch_alter_table('financial_document', schema=None) as batch_op:\n        # Agregar nuevas columnas DIAN\n        batch_op.add_column(\n            sa.Column('dian_status', sa.String(50), nullable=True, server_default='PENDING')\n        )\n        batch_op.add_column(\n            sa.Column('dian_response_status', sa.String(255), nullable=True)\n        )\n        batch_op.add_column(\n            sa.Column('dian_response_description', sa.Text, nullable=True)\n        )\n        batch_op.add_column(\n            sa.Column('cud_code', sa.String(100), nullable=True)\n        )\n    \n    # Migrar datos de columnas antiguas SUNAT a nuevas DIAN\n    # (Ejecutar como raw SQL para compatibilidad)\n    op.execute(\n        \"UPDATE financial_document SET dian_status = sunat_status WHERE sunat_status IS NOT NULL\"\n    )\n    op.execute(\n        \"UPDATE financial_document SET cud_code = cdr_code WHERE cdr_code IS NOT NULL\"\n    )\n\ndef downgrade() -> None:\n    with op.batch_alter_table('financial_document', schema=None) as batch_op:\n        batch_op.drop_column('dian_status')\n        batch_op.drop_column('dian_response_status')\n        batch_op.drop_column('dian_response_description')\n        batch_op.drop_column('cud_code')\n```\n\n### Paso 2.3: Ejecutar Migraciones\n\n```bash\n# Verificar estado actual\nalembic current\n\n# Aplicar nuevas migraciones\nalembic upgrade head\n\n# Verificar que se aplicaron\nalembic history\n```\n\n---\n\n## FASE 3: Servicios y Adaptadores\n\n### Paso 3.1: Renombrar Servicio de Verificación\n\n**Cambiar:**\n- `src/infrastructure/adapters/sunat/realtime_verifier.py`\n- A: `src/infrastructure/adapters/dian/realtime_verifier.py`\n\n**Código:**\n```python\n# Cambiar clase\nclass SunatRealtimeVerifier -> class DianRealtimeVerifier\n\n# Cambiar atributos\ndef __init__(\n    self,\n    ruc_lookup_url: str,     # → nit_lookup_url\n    cpe_lookup_url: str,     # → documento_lookup_url\n    token: str,\n    timeout_seconds: int = 10,\n):\n```\n\n### Paso 3.2: Crear Adaptador DIAN para Invoices\n\n**Archivo:** `src/infrastructure/adapters/dian/invoice_generator.py`\n\n```python\nfrom datetime import datetime\nfrom decimal import Decimal\nfrom typing import Any\n\nclass InvoiceGenerator:\n    \"\"\"Generador de facturas electrónicas para DIAN (RADIAN).\"\"\"\n\n    def __init__(self, xsd_dir: str = \"schemas/dian\"):\n        self.xsd_dir = xsd_dir\n\n    def generate_audit_log_diario(\n        self,\n        company_nit: str,\n        period: str,  # \"YYYY-MM\"\n        entries: list[dict],\n    ) -> bytes:\n        \"\"\"Genera libro diario para auditoría DIAN.\"\"\"\n        # Implementar según formato DIAN\n        pass\n\n    def generate_audit_log_mayor(\n        self,\n        company_nit: str,\n        period: str,\n        lines: list[dict],\n    ) -> bytes:\n        \"\"\"Genera libro mayor para auditoría DIAN.\"\"\"\n        # Implementar según formato DIAN\n        pass\n```\n\n### Paso 3.3: Crear Adaptador RADIAN\n\n**Archivo:** `src/infrastructure/adapters/dian/radian_submission.py`\n\n```python\nclass RadianSubmissionService:\n    \"\"\"Servicio de envío a RADIAN (Resolución DIAN 000042/2020).\"\"\"\n\n    def __init__(self, token: str = \"\", timeout_seconds: int = 30):\n        self.token = token\n        self.timeout = timeout_seconds\n\n    def generate_registro_txt(\n        self,\n        company_nit: str,\n        period: str,  # \"YYYY-MM\"\n        operations: list[dict],\n    ) -> bytes:\n        \"\"\"Genera registro electrónico para RADIAN.\"\"\"\n        # Implementar según especificación DIAN\n        pass\n```\n\n---\n\n## FASE 4: Actualizar Repositorios\n\n### Paso 4.1: Actualizar LedgerRepository\n\n```python\n# Cambiar en src/infrastructure/repositories/ledger_repository.py\n\n# ANTES\nresult = await session.execute(\n    select(SunatSubmission).where(\n        SunatSubmission.tenant_id == tenant_id,\n        SunatSubmission.status == \"QUEUED\"\n    )\n)\n\n# DESPUÉS\nresult = await session.execute(\n    select(DianSubmission).where(\n        DianSubmission.tenant_id == tenant_id,\n        DianSubmission.status == \"QUEUED\"\n    )\n)\n```\n\n### Paso 4.2: Actualizar UnitOfWork\n\n```python\n# Cambiar en src/infrastructure/unit_of_work.py\n\nclass UnitOfWork:\n    def __init__(self, session_factory, tenant_id: str):\n        # ...\n        self.dian_submissions = DianSubmissionRepository(session)  # Nuevo\n        # self.sunat_submissions = ...  # Eliminar\n```\n\n---\n\n## FASE 5: Actualizar Tests\n\n### Paso 5.1: Cambiar Fixtures\n\n```python\n# tests/fixtures/accounting.py\n\nimport pytest\nfrom src.domain.models.accounting import DianSubmission\n\n@pytest.fixture\ndef dian_submission_factory():\n    def make_submission(**kwargs):\n        return DianSubmission(\n            tenant_id=kwargs.get(\"tenant_id\", \"tenant_001\"),\n            submission_type=kwargs.get(\"submission_type\", \"INVOICE\"),\n            endpoint_type=\"DIAN\",\n            status=\"PENDING\",\n            **{k: v for k, v in kwargs.items() \n               if k not in [\"tenant_id\", \"submission_type\"]}\n        )\n    return make_submission\n```\n\n### Paso 5.2: Actualizar Mocks\n\n```python\n# tests/mocks/dian_verifier.py\n\nclass MockDianRealtimeVerifier:\n    \"\"\"Mock para DianRealtimeVerifier (reemplaza SunatRealtimeVerifier).\"\"\"\n\n    async def validate_nit(self, nit: str) -> dict:\n        return {\n            \"nit\": nit,\n            \"razon_social\": \"Empresa Ejemplo S.A.S.\",\n            \"estado\": \"ACTIVO\",\n            \"verificacion_tributaria\": \"OK\",\n        }\n```\n\n---\n\n## FASE 6: Actualizar Configuración\n\n### Paso 6.1: Cambiar settings.py\n\n```python\n# src/config/settings.py\n\nclass Settings(BaseSettings):\n    # ❌ ELIMINAR (SUNAT)\n    # sunat_ruc_lookup_url: str = \"\"\n    # sunat_cpe_lookup_url: str = \"\"\n    # sunat_xsd_dir: str = \"schemas/sunat\"\n\n    # ✅ AGREGAR (DIAN)\n    dian_nit_lookup_url: str = os.getenv(\"DIAN_NIT_LOOKUP_URL\", \"\")\n    dian_documento_lookup_url: str = os.getenv(\"DIAN_DOCUMENTO_LOOKUP_URL\", \"\")\n    dian_lookup_token: str = os.getenv(\"DIAN_LOOKUP_TOKEN\", \"\")\n    dian_realtime_timeout_seconds: int = 10\n    dian_realtime_guard_enabled: bool = True\n    dian_guard_block_on_unavailable: bool = False\n    dian_rut_lookup_url: str = os.getenv(\"DIAN_RUT_LOOKUP_URL\", \"\")\n    dian_antecedentes_lookup_url: str = os.getenv(\"DIAN_ANTECEDENTES_LOOKUP_URL\", \"\")\n    dian_xsd_dir: str = \"schemas/dian\"\n\n    class Config:\n        env_file = \".env\"\n        env_file_encoding = \"utf-8\"\n```\n\n### Paso 6.2: Actualizar .env\n\n```bash\n# .env\n\n# ❌ ELIMINAR\n# SUNAT_RUC_LOOKUP_URL=...\n# SUNAT_CPE_LOOKUP_URL=...\n# SUNAT_LOOKUP_TOKEN=...\n\n# ✅ AGREGAR\nDIAN_NIT_LOOKUP_URL=https://api-dian.example.com/nit-search\nDIAN_DOCUMENTO_LOOKUP_URL=https://api-dian.example.com/documento-search\nDIAN_LOOKUP_TOKEN=token-dian-produccion\nDIAN_RUT_LOOKUP_URL=https://api-dian.example.com/rut-search\nDIAN_ANTECEDENTES_LOOKUP_URL=https://api-dian.example.com/antecedentes\n```\n\n---\n\n## FASE 7: Validación Final\n\n### Paso 7.1: Ejecutar Tests\n\n```bash\n# Ejecutar tests unitarios\npytest tests/ -v --tb=short\n\n# Ejecutar tests de integración\npytest tests/integration/ -v -k dian\n\n# Coverage\npytest tests/ --cov=src --cov-report=html\n```\n\n### Paso 7.2: Verificar No Hay Referencias SUNAT\n\n```bash\n# Búsqueda exhaustiva\ngrep -r \"sunat_\" src/ --include=\"*.py\" | grep -v \"#\" | wc -l  # Debe ser 0\ngrep -r \"SUNAT\" src/ --include=\"*.py\" | grep -v \"#\" | wc -l    # Debe ser 0\ngrep -r \"PCGE\" src/api/routes/ --include=\"*.py\" | wc -l       # Debe ser 0 (excepto alias)\n```\n\n### Paso 7.3: Desplegar a Staging\n\n```bash\n# Crear rama de feature\ngit checkout -b feat/consolidation-colombiana\ngit add .\ngit commit -m \"Consolidación DIAN: eliminar referencias SUNAT\"\n\n# Push y crear PR\ngit push origin feat/consolidation-colombiana\n\n# Esperar aprobación de code review\n# Mergear a develop\n# Desplegar a staging\nkubectl set image deployment/conta-pro api=cuenta-pro:consolidated-dian\n```\n\n---\n\n## CHECKLIST DE FINALIZACIÓN\n\n- [ ] Modelos ORM renombrados (SunatSubmission → DianSubmission)\n- [ ] Migraciones Alembic creadas y probadas\n- [ ] Servicios renombrados (SunatRealtimeVerifier → DianRealtimeVerifier)\n- [ ] Adaptadores DIAN creados (InvoiceGenerator, RadianSubmissionService)\n- [ ] Repositorios actualizados\n- [ ] UnitOfWork actualizado\n- [ ] Tests actualizados\n- [ ] Settings.py actualizado\n- [ ] .env actualizado\n- [ ] Tests ejecutados correctamente\n- [ ] No hay referencias SUNAT/PCGE en código activo\n- [ ] Documentación actualizada\n- [ ] Code review aprobado\n- [ ] Despliegue a staging completado\n- [ ] Validación en staging exitosa\n- [ ] Despliegue a producción completado\n\n---\n\n## SOPORTE Y RESOLUCIÓN DE PROBLEMAS\n\n### Error: \"Table 'sunat_submission' not found\"\n**Solución:** Ejecutar migraciones Alembic: `alembic upgrade head`\n\n### Error: \"No module named 'DianSubmission'\"\n**Solución:** Ejecutar `python -m pip install -e .` para recargar módulos\n\n### Error: \"Foreign key constraint violation\"\n**Solución:** Verificar que FinancialDocument.dian_submission_id está siendo actualizado en migraciones\n\n---\n\n**Documento Preparado:** 2026-06-06\n**Tiempo Estimado:** 2-3 horas\n**Nivel de Dificultad:** Medio\n**Requiere:** SQLAlchemy, Alembic, PostgreSQL\n